# Use hoisted node_modules layout (like npm) so Prisma generate writes
# .prisma/client to /app/node_modules/.prisma/client — a predictable path
# that Docker COPY --from=build can reference without virtual-store globs.
node-linker=hoisted
