# Env local do agente. Copie para `apps/agent/.env` — as mesmas variáveis
# existem no `.env` da raiz, usadas pelo compose. Rodando pelo `uv`, é este
# arquivo que vale.
#
# VAZIO = degradação explícita: o serviço sobe, /health responde 200 e as
# capacidades respondem AI_PROVIDER_NOT_CONFIGURED. Ver README.md.

# LM Studio local em desenvolvimento; Cloudflare AI Gateway em produção.
AI_BASE_URL=http://localhost:1234/v1

# O LM Studio dispensa credencial. Fora de localhost, ela é obrigatória.
AI_API_KEY=

# Um modelo por capacidade. Os nomes abaixo são os que o LM Studio local serve.
#
# ⚠️ Contra endpoint REMOTO valem DUAS listas de src/fatia_agent/allowed_models.py:
# ALLOWED_HOSTS para o host de AI_BASE_URL e ALLOWED_MODELS para os modelos.
# Nenhuma implica a outra — um gateway roteia para muitos fornecedores e muitos
# gateways servem o mesmo nome de modelo. Trocar qualquer uma troca o
# subprocessador que a /privacy nomeia, e as listas fazem essa troca passar por
# um PR (ADR 020). Contra endpoint local, como este, não há terceiro e elas não
# se aplicam.
AI_MODEL_TEXT=ornith-1.0-9b
AI_MODEL_VISION=google/gemma-4-12b-qat
AI_MODEL_EMBEDDING=text-embedding-nomic-embed-text-v1.5

AI_TIMEOUT_S=120
AI_MAX_RETRIES=2

# Segredo compartilhado com o apps/api, exigido no header X-Fatia-Agent-Key das
# rotas /recognize-meal e /chat. Com AI_BASE_URL local pode ficar vazio:
# inferência local não custa nada. Fora de localhost é obrigatório — sem ele a
# rota seria um proxy aberto para o gateway pago (ADR 018).
#   openssl rand -hex 32
AGENT_API_KEY=

# Endpoint do /mcp do apps/api — o ÚNICO destino para onde o Bearer do usuário
# sai daqui (ADR 021). Vazio = a rota /chat degrada com MCP_NOT_CONFIGURED; o
# resto do agente continua funcionando.
#
# ⚠️ Contra host que não é local, só https. O agente encaminha o token de acesso
# de quem está conversando para este endereço: sem TLS, ele trafega em texto
# puro, e a montagem do cliente recusa antes de a primeira chamada existir.
MCP_BASE_URL=http://localhost:3000/mcp

# Timeout das chamadas ao /mcp. Menor que AI_TIMEOUT_S de propósito: do outro
# lado está o nosso Postgres, não um modelo em CPU.
MCP_TIMEOUT_S=30
